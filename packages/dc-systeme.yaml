# =============================================================================
# VALEURS CALCULEES - agregat des deux packs, et conversions en energie
# =============================================================================
# PAQUET inclus par docan.yaml. Ne se compile pas seul.
# Rien ici n'est lu sur le bus : tout est derive des mesures de dc-pack1 et
# dc-pack2.
#
# CONVENTION DE NOMMAGE : le qualificatif est en SUFFIXE - "Capacite totale
#   Systeme" et non "Systeme Capacite totale". Home Assistant classant par
#   ordre alphabetique, les trois versions d'une meme grandeur se retrouvent
#   ainsi cote a cote :
#     Capacite totale Pack1 / Pack2 / Systeme
#   Pack1 < Pack2 < Systeme : l'agregat tombe naturellement en dernier.
#   Meme principe que les suffixes Ond1 / Ond2 du montage Growatt.
#
# POURQUOI L'AGREGAT EXISTE : le protocole PYLON n'expose AUCUNE trame
# d'ensemble. Il est fait pour interroger les modules un par un. Verifie par
# cinq voies en aout 2026 - ADR=02, INFO=02, INFO=FF, CID2=42 sans champ INFO,
# et Modbus adresse 0x02 : rien ne renvoie de vue systeme.
#
# TOUS LES CALCULS SONT GARDES PAR isnan : au demarrage, ou si un pack cesse
# de repondre, la somme ne doit PAS etre publiee. Publier la capacite d'un
# seul pack parce que l'autre manque serait pire que ne rien publier - c'est
# exactement le genre de valeur qu'une automatisation prendrait au serieux.
#
#   CE GARDE-FOU N'A ETE RENDU EFFECTIF QUE LE 11/09/2026. Les capteurs des
#   packs sont des template en update_interval: never : ils valent NaN au
#   demarrage puis ne le redeviennent JAMAIS d'eux-memes. Un pack muet gardait
#   sa derniere valeur, et les isnan ci-dessous ne voyaient donc rien - ils ne
#   protegeaient que le premier cycle suivant un redemarrage. Ce sont
#   maintenant dc-pack1 et dc-pack2 qui republient explicitement NAN apres
#   trois cycles sans reponse ; voir l'encadre en tete de dc-pack1.yaml.
#   Rien n'a change ici : c'est la condition d'entree qui est devenue vraie.
#
# =============================================================================
# CONVERSION EN ENERGIE - tension NOMINALE, pas instantanee
# ---------------------------------------------------------------------------
# Une capacite en Ah ne devient des kWh qu'avec une tension. Deux choix
# possibles, et un seul defendable :
#
#   TENSION NOMINALE, 51.2 V = 16 cellules LiFePO4 a 3.2 V   <-- retenu
#     Chiffre stable et comparable, c'est la convention des fiches techniques.
#     325.87 Ah x 51.2 V = 16.68 kWh par pack, 33.37 kWh pour l'ensemble.
#
#   TENSION INSTANTANEE
#     Rejetee : elle varie de 43 a 57 V selon l'etat de charge et le courant.
#     L'energie "totale" d'un pack sauterait de 20 % sans que rien ne change
#     physiquement, et deux releves ne seraient pas comparables.
#
# CE QUE CE CHIFFRE N'EST PAS : une promesse. L'energie reellement restituee
#   est inferieure - la tension s'affaisse sous charge, le rendement n'est pas
#   parfait, et on ne descend jamais a 0 % (Prog 21 coupe a 20 % cote
#   onduleur). C'est une valeur de plaque, utile pour dimensionner et pour
#   comparer, pas pour predire une autonomie au watt pres.
#
# OU SONT CES ENTITES - reorganise le 12/09/2026.
#   Les versions Pack1 et Pack2 etaient definies ICI, par commodite : les
#   ecrire au meme endroit evitait de toucher aux deux paquets. Le resultat
#   est qu'on cherchait une entite Pack1 dans dc-pack1.yaml sans l'y trouver.
#   Elles sont reparties dans leurs paquets respectifs, aupres des capacites
#   en Ah dont elles derivent. Ce fichier ne garde que les deux versions
#   Systeme - c'est sa raison d'etre.
#
#   AUCUN NOM D'ENTITE N'A CHANGE : les identifiants Home Assistant derivent
#   du nom, pas du fichier. L'historique est continu, rien a renommer.
#
#   Le facteur 51.2 est devenu la substitution tension_nominale, declaree
#   dans docan.yaml : il servait dans un seul fichier, il en sert trois.
# =============================================================================
# L'ANOMALIE "10 POUR 1" - deux affirmations retirees le 26/08/2026
# ---------------------------------------------------------------------------
# Des que le pack 2 a pu parler lui-meme, voir le detail dans dc-pack2.yaml :
#   - le pack 2 pese 325.87 Ah, comme le pack 1. Les "32.5 Ah" venaient d'une
#     soustraction, jamais d'une mesure.
#   - le desequilibre de courant mesure est de 2.7 pour 1 au repos, pas de 10.
#
# "Repartition Pack1 Systeme" donne la part du courant total prise par le
# premier pack. Sur deux packs identiques et sains, elle doit osciller autour
# de 50 %.
#
# MAIS ELLE N'EST INTERPRETABLE QUE SOUS CHARGE. A quelques amperes, la
# repartition est gouvernee par des differences de quelques millivolts et
# milliohms - la moindre asymetrie de cosse produit un 70/30 sans que cela
# signifie quoi que ce soit.
#
# =============================================================================
# COMMENT LIRE CETTE REPARTITION - grille refaite le 12/09/2026
# ---------------------------------------------------------------------------
# CE QUI ETAIT ECRIT ICI, et qui est retire :
#   "vers 50-60 % a 40 A ....... normal, fin du dossier"
#   "vers 85 % a fort courant .. resistance de contact reelle, reprendre le
#                                cablage du pack 2"
#
#   Cette grille supposait qu'un RELEVE ISOLE a fort courant tranche. Les
#   mesures l'ont dementie : le partage OSCILLE sur plusieurs jours, avec des
#   INVERSIONS DE SENS. Or une asymetrie de resistance est fixe - elle ne peut
#   pas changer de signe. Ce qui oscille est donc autre chose, et un seul
#   releve ne separe pas les deux causes.
#
# LE MODELE. Deux branches sur la meme barre, chacune une force
#   electromotrice OCV et une resistance de chemin R - interne, cablage,
#   cosses, disjoncteur confondus. Convention du fichier : positif = charge.
#
#     I1 - I2  =  I_total x (R2 - R1)/(R1 + R2)  -  2 x (OCV1 - OCV2)/(R1 + R2)
#                 \_________ pente __________/      \____ terme constant ____/
#
#   TERME CONSTANT - l'ecart de tension a vide. C'est un DECALAGE EN AMPERES
#     qui ne depend PAS de la charge. Il derive lentement au fil des jours a
#     mesure que les SOC se rapprochent puis s'ecartent : c'est lui qui fait
#     osciller la repartition et croiser les courbes. Il se resorbe seul, le
#     pack le plus plein alimentant l'autre jusqu'a egalisation.
#
#   PENTE - l'asymetrie de resistance. Elle est PROPORTIONNELLE au courant
#     total. C'est le seul terme qui justifierait de demonter une cosse.
#     Son signe designe le chemin le plus resistant : pente negative, le
#     pack 1 prend proportionnellement moins a mesure que la charge monte,
#     donc R1 > R2.
#
# CE QU'IL FAUT MESURER : non pas un pourcentage, mais (I1 - I2) RELEVE A
#   PLUSIEURS COURANTS TOTAUX. La droite separe proprement les deux termes, ce
#   qu'aucun releve isole ne permet :
#     pente nulle ....... branches equilibrees, quel que soit le decalage
#                         constant observe au repos. Dossier clos.
#     pente non nulle ... asymetrie reelle, et son signe designe le coupable.
#
# POURQUOI LE POURCENTAGE TROMPE : a ecart de tension constant, l'ecart en
#   AMPERES ne bouge pas quand la charge augmente - mais rapporte a un total
#   plus grand, il donne un pourcentage plus proche de 50 %. Un partage qui
#   "s'ameliore" a 40 A n'a donc rien prouve : c'est de l'arithmetique, pas une
#   guerison. C'est exactement le piege que l'ancienne grille tendait.
#
# LE TEST RAPIDE, sans tracer de droite : un ECHELON de courant. Allumer une
#   charge et relever dI1 et dI2 sur quelques secondes. Sur ce laps de temps
#   l'OCV ne bouge pas, le terme constant s'annule, et il reste simplement
#     dI1 x R1 = dI2 x R2
#   Le rapport des variations donne directement le rapport inverse des
#   resistances. C'est le meme raisonnement que la pente, en une seule mesure.
#
# LIMITES DU MODELE, pour ne pas lui faire dire plus qu'il ne dit : il suppose
#   les deux branches lineaires et R constante sur la plage exploree. La
#   resistance interne d'une cellule varie avec le SOC et la temperature, donc
#   les points doivent etre releves dans une meme session, pas etales sur des
#   jours - sinon la derive de l'OCV se reinvite dans la pente.
#
# FERMER LE SYSTEME : trois observables, trois inconnues.
#   L'ordonnee a l'origine vaut -2 x (OCV1 - OCV2)/(R1 + R2), et OCV1 - OCV2
#   se lit directement sur "Ecart tension packs" AU REPOS, une fois son offset
#   de mesure retire - voir l'entite, -52 mV etabli le 12/09/2026. D'ou
#   R1 + R2. La pente donne alors R2 - R1, et les deux resistances de chemin
#   sortent separement. Meme session pour les trois, sinon la derive d'OCV
#   brouille tout.
#
#   PREMIERS INDICES, releves au demarrage de la charge du 12/09 a 11:09 -
#   deux points seulement, echantillons a 30 s, a prendre pour ce qu'ils sont :
#     repos   I1 = -0.6   I2 = -1.4   le pack 2 DONNE plus
#     charge  I1 = 11.1   I2 =  8.8   le pack 2 RECOIT moins
#   C'est la signature des deux termes : le constant domine au repos, la
#   pente sous charge. Estimation grossiere : R2/R1 ~ 1.15, et OCV2 > OCV1
#   alors que le pack 2 est annonce MOINS plein - l'un des deux compteurs de
#   SOC derive. La droite a cinq ou six courants differents tranchera.
#
# ET SI C'EST LE SHUNT QUI MENT : la pince amperemetrique sur le cable negatif
#   du pack 2 reste le juge de paix. Si elle lit le meme courant que le pack 1
#   alors que le BMS en annonce moins, c'est la MESURE qui est fausse et non
#   le cablage - et aucune droite tracee a partir de ces valeurs n'aurait de
#   sens.
# =============================================================================
#
# LES VALEURS ABSOLUES MENTAIENT - corrige le 11/09/2026.
#   Le calcul etait fabs(i1) / (fabs(i1) + fabs(i2)). Quand les deux packs
#   vont dans le MEME sens il est exact, et le calcul algebrique qui le
#   remplace rend rigoureusement les memes chiffres : l'historique reste
#   comparable, rien ne se decale dans les graphiques.
#
#   Mais quand les signes S'OPPOSENT - +1.0 A d'un cote, -1.0 A de l'autre -
#   les valeurs absolues affichaient 50 %, soit le partage parfait, alors
#   qu'il ne passe RIEN vers l'onduleur et qu'un pack se vide dans l'autre.
#   Le pire symptome portait le meilleur chiffre.
#
#   Et ce n'est pas un cas d'ecole : c'est precisement le regime attendu a
#   consommation nulle, quand le pack le plus plein alimente le moins plein.
#   Exactement ce qu'on cherche a caracteriser depuis une semaine.
#
#   DESORMAIS : denominateur algebrique i1 + i2, c'est-a-dire le vrai courant
#   systeme, et rien n'est publie si les deux packs vont en sens contraire
#   au-dela de 0.3 A. L'entite "Regime packs Systeme" nomme alors ce qui se
#   passe - sinon une case vide serait indiscernable d'un pack muet.
# =============================================================================

sensor:

  # --- Agregat des deux packs, en Ah ----------------------------------------

  - platform: template
    name: "${upper_devicename} Capacite totale Systeme"
    id: sys_total
    unit_of_measurement: "Ah"
    state_class: measurement
    accuracy_decimals: 2
    icon: mdi:battery-high
    lambda: |-
      if (isnan(id(p1_total).state) || isnan(id(p2_total).state)) return NAN;
      return id(p1_total).state + id(p2_total).state;
    update_interval: 30s

  - platform: template
    name: "${upper_devicename} Capacite restante Systeme"
    id: sys_remaining
    unit_of_measurement: "Ah"
    state_class: measurement
    accuracy_decimals: 2
    icon: mdi:battery-medium
    lambda: |-
      if (isnan(id(p1_remaining).state) || isnan(id(p2_remaining).state)) return NAN;
      return id(p1_remaining).state + id(p2_remaining).state;
    update_interval: 30s

  - platform: template
    name: "${upper_devicename} SOC Systeme"
    id: sys_soc
    unit_of_measurement: "%"
    device_class: battery
    state_class: measurement
    accuracy_decimals: 1
    lambda: |-
      if (isnan(id(p1_remaining).state) || isnan(id(p2_remaining).state) ||
          isnan(id(p1_total).state)     || isnan(id(p2_total).state)) return NAN;
      float tot = id(p1_total).state + id(p2_total).state;
      if (tot < 0.1f) return NAN;
      return (id(p1_remaining).state + id(p2_remaining).state) / tot * 100.0f;
    update_interval: 30s

  # Somme algebrique : positif en charge, negatif en decharge.
  - platform: template
    name: "${upper_devicename} Courant Systeme"
    id: sys_current
    unit_of_measurement: "A"
    device_class: current
    state_class: measurement
    accuracy_decimals: 1
    lambda: |-
      if (isnan(id(p1_current).state) || isnan(id(p2_current).state)) return NAN;
      return id(p1_current).state + id(p2_current).state;
    update_interval: 30s

  - platform: template
    name: "${upper_devicename} Puissance Systeme"
    id: sys_power
    unit_of_measurement: "W"
    device_class: power
    state_class: measurement
    accuracy_decimals: 0
    lambda: |-
      if (isnan(id(p1_power).state) || isnan(id(p2_power).state)) return NAN;
      return id(p1_power).state + id(p2_power).state;
    update_interval: 30s

  # --- Les memes capacites, en energie --------------------------------------
  # x tension_nominale / 1000, la substitution declaree dans docan.yaml. Voir
  # l'encadre en tete de fichier : tension NOMINALE et non instantanee, sinon
  # le chiffre sauterait de 20 % sans que rien ne change physiquement.
  #
  # SEULES LES VERSIONS SYSTEME SONT ICI. Les versions Pack1 et Pack2 sont
  # dans dc-pack1.yaml et dc-pack2.yaml, aupres des capacites en Ah dont elles
  # derivent.

  - platform: template
    name: "${upper_devicename} Energie restante Systeme"
    unit_of_measurement: "kWh"
    device_class: energy_storage
    state_class: measurement
    accuracy_decimals: 2
    icon: mdi:battery-medium
    lambda: |-
      if (isnan(id(p1_remaining).state) || isnan(id(p2_remaining).state)) return NAN;
      return (id(p1_remaining).state + id(p2_remaining).state) * ${tension_nominale}f / 1000.0f;
    update_interval: 30s

  - platform: template
    name: "${upper_devicename} Energie totale Systeme"
    unit_of_measurement: "kWh"
    device_class: energy_storage
    state_class: measurement
    accuracy_decimals: 2
    icon: mdi:battery-high
    lambda: |-
      if (isnan(id(p1_total).state) || isnan(id(p2_total).state)) return NAN;
      return (id(p1_total).state + id(p2_total).state) * ${tension_nominale}f / 1000.0f;
    update_interval: 30s

  # --- Diagnostic de l'anomalie ---------------------------------------------

  # Part du courant systeme prise par le pack 1. Trois cas ou elle n'est PAS
  # publiee, tous documentes dans l'en-tete :
  #   un pack muet ......... isnan, cf. dc-pack1.yaml
  #   sous 0.5 A systeme ... le rapport sauterait dans tous les sens
  #   sens opposes ......... un pack alimente l'autre, ce n'est plus un partage
  # Le denominateur est ALGEBRIQUE : i1 + i2, pas fabs(i1) + fabs(i2). Les deux
  # sont identiques quand les packs vont dans le meme sens - donc pas de
  # rupture dans l'historique - mais seul l'algebrique refuse de donner 50 %
  # a deux courants qui s'annulent.
  #
  # POUR CONCLURE QUOI QUE CE SOIT sur le cablage, ce pourcentage ne suffit
  # pas : voir "COMMENT LIRE CETTE REPARTITION" en tete de fichier. C'est
  # (I1 - I2) releve a plusieurs courants totaux qui tranche, pas un
  # pourcentage releve une fois.
  - platform: template
    name: "${upper_devicename} Repartition Pack1 Systeme"
    id: sys_repartition
    unit_of_measurement: "%"
    state_class: measurement
    accuracy_decimals: 0
    icon: mdi:scale-balance
    entity_category: diagnostic
    lambda: |-
      if (isnan(id(p1_current).state) || isnan(id(p2_current).state)) return NAN;
      float i1 = id(p1_current).state;
      float i2 = id(p2_current).state;
      // Sens opposes au-dela de la bande morte : transfert entre packs.
      if (i1 >  0.3f && i2 < -0.3f) return NAN;
      if (i1 < -0.3f && i2 >  0.3f) return NAN;
      float tot = i1 + i2;
      if (fabs(tot) < 0.5f) return NAN;
      return i1 / tot * 100.0f;
    update_interval: 30s

  # L'ECART EN AMPERES, la grandeur qui se lit vraiment - cf. l'encadre en
  # tete de fichier. A ecart de tension a vide constant, elle ne bouge PAS
  # quand la charge augmente, alors que le pourcentage se rapproche de 50 %
  # par simple arithmetique. Tracee contre "Courant Systeme", sa pente donne
  # l'asymetrie de resistance et son ordonnee a l'origine l'ecart de tension.
  # Positive : le pack 1 prend plus que le pack 2, dans le sens du courant.
  - platform: template
    name: "${upper_devicename} Ecart courant packs Systeme"
    id: sys_ecart_i
    unit_of_measurement: "A"
    device_class: current
    state_class: measurement
    accuracy_decimals: 1
    icon: mdi:unfold-more-vertical
    entity_category: diagnostic
    lambda: |-
      if (isnan(id(p1_current).state) || isnan(id(p2_current).state)) return NAN;
      return id(p1_current).state - id(p2_current).state;
    update_interval: 30s

  # Ecart de tension entre les deux packs, POSITIF = pack 1 plus haut.
  #
  # ELLE CONTIENT UN OFFSET DE MESURE, etabli le 12/09/2026 : a 0.0 A sur les
  #   deux packs, disjoncteurs fermes, packs en parallele sur la barre - donc
  #   physiquement au MEME potentiel - cette entite lisait -52 mV, stable a
  #   +/-5 mV sur toute une nuit (plage -47 a -58). Un ecart reel de cette
  #   taille aurait fait circuler ~1 A entre les packs ; on lisait 0.0.
  #   Les deux BMS ne lisent donc pas pareil sur le meme noeud : le pack 2
  #   affiche ~52 mV de plus. Lequel a raison, un voltmetre sur la barre le
  #   dira. En attendant, toute lecture se corrige de +0.052 V avant d'etre
  #   interpretee.
  #
  #   NON VERIFIE : que l'offset soit constant en tension. Un defaut de GAIN
  #   d'ADC se verrait differemment a 51 V et a 57 V. Prochain point de
  #   mesure : la fin de charge, au repos, vers 57 V.
  #
  # Une fois l'offset retire, AU REPOS c'est l'ecart de tension a vide,
  # OCV1 - OCV2 : le terme constant de la grille en tete de fichier. Sous
  # courant s'y ajoutent les chutes internes I x R de chaque pack.
  - platform: template
    name: "${upper_devicename} Ecart tension packs Systeme"
    id: sys_ecart_v
    unit_of_measurement: "V"
    state_class: measurement
    accuracy_decimals: 3
    icon: mdi:flash-alert
    entity_category: diagnostic
    lambda: |-
      if (isnan(id(p1_voltage).state) || isnan(id(p2_voltage).state)) return NAN;
      return id(p1_voltage).state - id(p2_voltage).state;
    update_interval: 30s

text_sensor:

  # NOMME le regime des deux packs. Sa premiere raison d'etre est d'expliquer
  # pourquoi "Repartition Pack1 Systeme" n'est pas publiee : sans elle, une
  # case vide au repos serait indiscernable d'un pack qui ne repond plus.
  #
  # SIGNE : positif = le pack se CHARGE, negatif = il se decharge. Un transfert
  #   est donc nomme d'apres celui qui donne : i1 < 0 et i2 > 0 se lit
  #   "Transfert Pack1 vers Pack2".
  #
  # BANDE MORTE DE 0.3 A par pack, sans quoi le bruit de mesure ferait basculer
  #   l'etat a chaque cycle autour de zero. Elle vaut aussi pour le calcul de
  #   repartition ci-dessus : memes seuils, memes conclusions.
  #
  # "Pas de reponse" N'EST PAS "pas encore lu". Pendant les 30 premieres
  #   secondes apres un demarrage, les courants valent NaN parce qu'aucune
  #   trame n'est encore arrivee - aucun pack n'a failli. Les compteurs
  #   d'echec de dc-pack1 et dc-pack2 font la difference : au-dela de 3, c'est
  #   un vrai silence ; en-deca, c'est l'initialisation. Sans ce test l'entite
  #   annoncerait une panne a chaque flash.
  - platform: template
    name: "${upper_devicename} Regime packs Systeme"
    id: sys_regime
    icon: mdi:swap-horizontal
    entity_category: diagnostic
    lambda: |-
      if (isnan(id(p1_current).state) || isnan(id(p2_current).state)) {
        if (id(p1_miss) >= 3 || id(p2_miss) >= 3) return {"Pas de reponse"};
        return {"Initialisation"};
      }
      float i1 = id(p1_current).state;
      float i2 = id(p2_current).state;
      const float S = 0.3f;
      if (i1 >  S && i2 < -S) return {"Transfert Pack2 vers Pack1"};
      if (i1 < -S && i2 >  S) return {"Transfert Pack1 vers Pack2"};
      if (i1 >  S || i2 >  S) return {"Charge"};
      if (i1 < -S || i2 < -S) return {"Decharge"};
      return {"Repos"};
    update_interval: 30s

  # Reunit les deux alarmes en une seule entite a surveiller.
  # Rappel : "T2" est un bruit de fond permanent sur ce BMS - champ non
  # implemente, cf. l'en-tete de docan.yaml. Tout le reste compte.
  - platform: template
    name: "${upper_devicename} Alarmes Systeme"
    id: sys_alarm
    icon: mdi:alert-circle-outline
    lambda: |-
      std::string a = id(p1_alarm_txt).state;
      std::string b = id(p2_alarm_txt).state;
      if (a.empty() && b.empty()) return {};
      if (a.empty()) a = "?";
      if (b.empty()) b = "?";
      if (a == "OK" && b == "OK") return {"OK"};
      return {"P1:" + a + "  P2:" + b};
    update_interval: 30s
